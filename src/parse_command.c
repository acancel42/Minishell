#include "minishell.h"
/*
t_token	token_init(char	*src)
{
	t_token token;
	int		i;

	i = 0;
	if (src[i] == '=')
	{
		token.type = EQUAL;
		token.value = ft_strdup("=");
		return (token);
	}
	else if (ft_isdigit(src[i]))
	{
		while (ft_isdigit(src[i++]))
			ft_strjoin(token.value, &src[i]);
		return (token);
	}
	else
		exit(EXIT_FAILURE);
}

int	main(int argc, char **argv)
{
	t_token	token;

	(void)argc;
	token = token_init(argv[1]);
	printf("%s", token.value);
}
*/
